
## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

## Docker Compose

Run `docker compose -f docker-compose.dev.yml up -d` to start the development server.